Description:
    Generates the migration file required to create the portfolio model

Example:
    rails generate spina_portfolio:install

    This will create:
        db/migrate/timestamp_create_spina_portfolio_tables.rb
