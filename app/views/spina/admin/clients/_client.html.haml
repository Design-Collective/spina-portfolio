%li.dd-item
  %div.dd-item-inner
    = link_to spina.edit_admin_client_path(client), class: 'button button-link button-small sortable-link' do
      = client.name
    %span.pull-right
      = link_to spina.edit_admin_client_path(client), class: 'button button-small button-link dd-nodrag' do
        = icon('pencil-outline')
        = t('spina.edit')
